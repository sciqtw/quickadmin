import qchart from './components/qchart.vue'
// import VChart, { THEME_KEY } from "vue-echarts";

Quick.booting(Vue => {
    // Vue.component('v-chart', VChart)
    Vue.component('q-chart', qchart)
})
